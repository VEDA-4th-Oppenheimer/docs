# 문서 작성 규칙

## 0. 한 문서는 그것만 읽고도 이해돼야 한다

이 저장소의 문서는 **각각이 독립된 보고서**입니다. 필요한 배경은 문서 안에 쓰고,
다른 문서로 넘기지 마세요.

- "○○ 문서 참고"로 때우지 않는다. 필요한 내용이면 **여기에 다시 쓴다.**
- 문서끼리 링크로 엮지 않는다. 목차는 루트 `README.md` 하나면 충분하다.
- 외부 위키·노션 링크를 본문에 넣지 않는다. **읽는 사람이 그 링크를 못 열 수 있다.**
  내용이 필요하면 텍스트로 옮겨 적는다.

읽는 사람이 팀원만은 아닙니다. 처음 보는 사람이 이 저장소만 받아도 프로젝트를 이해할 수
있어야 합니다.

## 1. 어디에 쓸지

| 쓰려는 내용 | 위치 |
|---|---|
| 시스템 전체 구조 | `overview/` |
| 여러 파트가 함께 지키는 규격·계약 | `interfaces/` |
| 특정 컴포넌트 안에서만 의미 있는 설계 | `components/<이름>/` |
| 따라 하면 되는 절차 (환경설정·빌드·트러블슈팅) | `guides/` |
| "왜 A 대신 B를 골랐나" | `decisions/` (ADR) |
| 회의록 | `meetings/` |

애매하면 `guides/`에 넣고 나중에 옮기세요. 안 쓰는 것보다 낫습니다.

## 2. 파일 이름

- **소문자 + 하이픈**: `stm32-rpi-uart.md` (O) / `STM32 RPI UART.md` (X)
- 한글 파일명은 GitHub URL이 깨져 보이고 링크 공유가 불편합니다.
  **파일명은 영문, 문서 제목(`# ...`)은 한글**로 씁니다.
- 회의록은 날짜 prefix: `meetings/2026-08-21-regular.md`
- ADR은 번호 prefix: `decisions/0001-why-yocto.md`

## 3. 문서 형식

- 첫 줄은 `# 제목` 하나.
- 표와 코드블록을 적극적으로 씁니다. 줄글보다 표가 읽기 쉽습니다.
- 다이어그램은 mermaid 또는 ASCII로 **본문 안에** 그립니다. GitHub가 그대로 렌더합니다.
- 이미지가 꼭 필요하면 `assets/`에 넣고 상대경로로 링크합니다.
- 폴더를 새로 만들면 그 안에 `README.md`를 함께 만듭니다.
  git은 빈 폴더를 추적하지 않아서, README가 없으면 폴더 자체가 push되지 않습니다.

## 4. 값을 적을 때

CMD 번호, 구조체 크기, 핀 번호 같은 값은 **코드가 진실 소스**입니다. 문서에 적을 때는
어느 코드를 기준으로 한 시점인지 함께 적으세요.

```
> 기준: STM32 main/003e483 · 2026-08-21
```

값만 적고 끝내지 말고 **왜 그 값인지**를 함께 쓰면 나중에 바꿔도 되는지 판단할 수 있습니다.

## 5. 새 문서를 추가하면

루트 [README.md](README.md) 목차에 한 줄 추가하세요. 목차에 없는 문서는 아무도 못 찾습니다.

## 6. 브랜치

문서만 바꾸는 작은 수정은 `main`에 직접 push해도 됩니다.
구조를 바꾸거나 여러 문서를 손대면 브랜치를 파고 PR을 올려주세요.
