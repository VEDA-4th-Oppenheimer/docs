# 문서 작성 규칙

## 0. 코드에 있는 값을 문서에 베끼지 않는다

CMD 번호, 구조체 레이아웃, 핀 번호, 상수 — 이런 값의 진실 소스는 **코드**입니다.
문서에 옮겨 적으면 사본이 하나 더 늘고, 코드 쪽 CI는 문서를 보지 않으므로
어긋나도 아무도 모릅니다. (`shared/protocol.h`는 이미 사본끼리 v5/v6로 어긋난 적이 있습니다.)

문서는 코드가 담지 못하는 것을 담습니다 — **왜 그렇게 했는지, 어떤 순서로 쓰는지,
안 될 때 어디부터 보는지.** 값이 꼭 필요하면 "스냅샷"임을 밝히고 코드를 링크하세요.

## 1. 어디에 쓸지

| 쓰려는 내용 | 위치 |
|---|---|
| 시스템 전체 구조, 요구사항, 용어 | `overview/` |
| 컴포넌트 사이의 데이터 형식·프로토콜 | `interfaces/` |
| "왜 A 대신 B를 골랐나" | `decisions/` (ADR) |
| 환경 설정, 빌드 절차, 트러블슈팅 | `guides/` |
| 특정 컴포넌트 안에서만 의미 있는 설계 | `components/<이름>/` |
| 회의록 | `meetings/` |

애매하면 `guides/`에 넣고 나중에 옮기세요. 안 쓰는 것보다 낫습니다.

## 2. 파일 이름

- **소문자 + 하이픈**: `stm32-rpi-uart.md` (O) / `STM32 RPI UART.md` (X)
- 한글 파일명은 GitHub URL이 깨져 보이고 링크 공유가 불편합니다.
  **파일명은 영문, 문서 제목(`# ...`)은 한글**로 씁니다.
- 회의록은 날짜 prefix: `2026-08-20-정기회의.md` → `meetings/2026-08-20-regular.md`
- ADR은 번호 prefix: `0001-why-yocto.md`

## 3. 문서 형식

- 첫 줄은 항상 `# 제목` 하나.
- 이미지는 `assets/`에 넣고 상대경로로 링크: `![구조도](../assets/architecture.png)`
- 코드 조각은 필요한 만큼만. 파일 전체를 붙여넣지 말고 해당 레포를 링크하세요.
- 폴더를 새로 만들면 그 안에 `README.md`를 함께 만듭니다.
  git은 빈 폴더를 추적하지 않아서, README가 없으면 폴더 자체가 push되지 않습니다.

## 4. 새 문서를 추가하면

루트 [README.md](README.md) 목차에 링크를 추가하세요. 목차에 없는 문서는 아무도 못 찾습니다.

## 5. 브랜치

문서만 바꾸는 작은 수정은 `main`에 직접 push해도 됩니다.
구조를 바꾸거나 여러 문서를 손대면 브랜치를 파고 PR을 올려주세요.
